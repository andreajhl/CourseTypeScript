(() => {
    // T or F, according to config it can be null or undefined.
    //By default, undefined is assignable to a variable of type null.
    let isSuperman: boolean = true; 
    console.log(isSuperman);
})();