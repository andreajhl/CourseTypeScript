(() => {
    //crea un objeto con las keys de 0 en adelante y un valor por defecto de 1 en la posición 0
    enum AudioLevel {
        min = 1,
        medium,
        max = 10
    }

    const currentAudio = AudioLevel.medium
})();